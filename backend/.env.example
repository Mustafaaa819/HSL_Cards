# No secrets are needed yet for Phase 0 (scaffolding only).
# This file exists so the load_dotenv() convention is in place before
# real config (DB URLs, room secrets, etc.) shows up in later phases.
