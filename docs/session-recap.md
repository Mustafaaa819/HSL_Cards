Project: HSL Cards — multiplayer climbing card game (FastAPI + WebSocket backend, React/Vite frontend). Session recap below, paste this into a new chat to continue.

1. Started by auditing the PREVIOUS session's own handoff recap against the real repo — it was partly wrong. `game.py`/`game_ws.py`/`docs/WS_PROTOCOL.md` were claimed uncommitted but were actually already on `main` (commit `960818c`). `CLAUDE.md`/`docs/RULES.md` were claimed to have "cosmetic CRLF issues" but were actually 100% CRLF noise with zero real content difference from `HEAD` once normalized. `backend/static/` (build output) was untracked and about to get swept into a commit — added to `.gitignore` instead. What was genuinely uncommitted (52 recolored dark-theme card SVGs in `frontend/src/assets/cards/`, the "Throw multiples" frontend UI, `vite.config.js`/CORS dev-workflow fixes, `Dockerfile` attempt) got committed as `13a878b`. Lesson: verify a handoff recap against the real repo before trusting it, don't just relay it forward.

2. Two backend rule changes, done properly: (a) picking up the pile (voluntary or forced) no longer ends the turn — the picker must immediately throw one more card; (b) the 2 became a real bonus action — play it, then you're forced to throw again (chains if the follow-up is also a 2), and in the blind phase a flipped 2 auto-chains into flipping your next blind card (can win the game outright this way). Both share one engine primitive (`Game.pending_throw` / `_arm_followup` in `backend/app/engine/game.py`). This was NOT a bug fix — it reversed what `RULES.md` previously said explicitly (pickup ends turn, power cards get no bonus turn) — confirmed via extensive clarifying questions before writing the prompt, including edge cases (deck-phase last-card waiver, AFK-stuck-mid-2 fallback via a system-forced pickup). Verified for real afterward: installed deps fresh, ran the actual test suite myself (148 passed, up from 134), traced the state machine by hand, found and closed one real test-coverage gap (a 2 played from face-up that empties it, handing the follow-up down into a forced blind flip — code was already correct, just untested). Committed as `023d7bd`.

3. Frontend UI phase, five sessions deep, all still UNCOMMITTED (see item 9 — this is the most important open fact). In order:
   - **Card art + typography + depth** (`docs/phase-prompts/visual-polish-and-card-art.md`): wired the 52 real SVG card faces into `Card.jsx` (previously placeholder rank+suit text), added a condensed display font (Oswald) for ranks only, added real shadow/elevation/press-feedback depth — explicitly NOT a new color palette, to protect the gold-reserved-for-power-cards signal.
   - **Circular table + one-time dealing animation** (`docs/phase-prompts/round-table-layout-and-dealing.md`): true circular seating (not just an arc) specifically so J's reverse-direction effect is spatially legible — capped at 5 players max, not the theoretical 11 from two-deck games. A one-time cosmetic client-side "deal" animation plays only on a genuine fresh match start (signaled via `App.jsx`'s lobby `onStarted` callback, never on reconnect) — server dealing itself never changed, purely a visual replay of already-known data.
   - **Premium glassmorphism pass** (`docs/phase-prompts/premium-glass-table.md`): user uploaded a Claude-Design mockup zip (3 options, all LIGHT-themed) and wanted "1a" applied. Caught and flagged before executing: the mockups' literal light colors would have made the white/red-ink card art illegible and clashed with the dark theme everywhere else — translated the LAYOUT and glassmorphism TECHNIQUE onto the existing dark token system instead of copying colors literally. Added fanned/overlapping hand cards (rotation on a wrapper span, not the `Card` element, so selected/rejected states keep working inside it).
   - **Declutter pass** (`docs/phase-prompts/declutter-table-and-felt.md` + a `-resume.md` for a usage-limit interruption): user's own status was being shown TWICE (a seat in the ring AND the full panel below) — removed your own seat from the ring entirely, opponents-only now, with the angle math deliberately kept as if your seat still existed (reserved as an invisible anchor) so the direction-ring geometry didn't warp. Idle seats lost all background/border/blur — only the current-turn seat gets a box now. Added a real wood-and-poker-chip table-frame PNG (`frontend/src/assets/table/table-frame.png` — I could not extract this from a pasted chat image myself; the user had to save the file into the project directly) layered over a new deep-emerald felt color (`--felt: #1e4534`). I incorrectly warned the PNG's interior was opaque white; Claude Code checked the actual alpha channel and found it was already transparent — corrected in the resume prompt.
   - **Background lighten + hand-wrap** (`docs/phase-prompts/background-lighten-and-hand-wrap.md`), just completed: nudged `--bg`/`--surface`/`--surface-raised` lighter (`#14181a`→`#1b2124` etc. — confirmed for real in the file, not just claimed), left `--card-face` alone on purpose so cards keep contrast against the lighter panels. Hand now wraps into multiple fan rows (`HAND_ROW_MAX = 6`, evened out so 9 cards renders 5+4 not 6+3) instead of horizontal-scrolling, since the rotate+negative-margin fan technique doesn't reflow via plain CSS wrap — each row is its own independent self-contained fan. Flight-animation tap-position lookup and multi-select were specifically verified to still work across rows.
   - **Known open imperfection, explicitly deferred, not yet fixed:** with a big hand wrapping to two rows, the `you-area` panel's bottom edge sits ~21px past the fold at 390×844 (every card IS visible, just some bottom chrome/padding isn't). Fixing it means taking height out of the table area, ruled out of scope for that pass. The user said "just one thing remaining, everything else is perfect" when ending this session — this overflow is my best guess at what that refers to, but it was not explicitly confirmed, so verify with them before assuming that's the actual remaining item.

4. Working method established across every one of these frontend passes, worth continuing: (1) ask real clarifying questions before writing a prompt when the request is genuinely ambiguous or trades off against something already built (I did this for the rule changes and the round-table scope, using `AskUserQuestion`); (2) write the Claude Code prompt to a `docs/phase-prompts/*.md` file grounded in the ACTUAL current code (read the real files first, don't guess at class names/structure); (3) after Claude Code reports back, independently verify — read the real diff, check brace balance, check the actual deploy timestamps (`frontend/dist` vs `backend/static` — this repeatedly caught undeployed work), spot-check the trickiest claims by hand (e.g. traced the circular-seat angle trigonometry myself to confirm "clockwise" was actually clockwise) rather than trusting the transcript summary. This caught real things: my own wrong assumption about the PNG's alpha channel, a missing test case in the rule-engine work, undeployed builds twice.

5. Claude Code / Fable 5 hit its usage limit mid-session THREE separate times this conversation (visual-polish pass, once during the declutter investigation, once more via an API connection drop mid-response on the declutter pass). Each time: if real edits had already landed, the fix was just verifying state and giving deploy instructions, not a new prompt. If it was cut off during pure investigation with zero edits made (the declutter case), a short resume prompt was written that hands forward only the ALREADY-CONFIRMED findings (e.g. the PNG alpha-channel result) so the next session doesn't re-burn budget re-deriving them, pointing back at the original spec doc rather than duplicating it.

6. Local dev / deploy workflow (unchanged from earlier in the project, still true): `cd frontend && npm run build` (PowerShell needs `;` not `&&` as a separator — the user is on Windows PowerShell 5.1), copy `dist/*` into `backend/static`, restart `uvicorn app.main:app --port 8000` from an activated venv in `backend/`, `cloudflared tunnel --url http://localhost:8000` for the public link (new random URL every restart, free tier). A "port 8000 already in use" error the user hit was NOT a regression from any of this UI work — it was just Claude Code's own already-deployed backend process still running and healthy, confirmed via `/health` returning `{"status":"ok"}` before telling the user to stand down rather than kill it.

7. This sandbox environment has a real, recurring quirk unrelated to the project itself: `.git/index.lock` (and other `.git/*.lock` files) can be created but not deleted (`rm`/`unlink` return "Operation not permitted") on this particular fuse-mounted folder — `mv`/rename works fine though. Every commit in this sandbox required renaming the stale lock out of the way first. Not relevant if working directly in the user's own terminal instead of through this sandbox.

8. Card art licensing note, still true: the 52 SVGs are a recolor of Chris Aguilar's LGPL-3.0 "Vector Playing Card Library" v3.2, provenance and exact color mapping documented in `frontend/src/assets/cards/NOTICE.md`.

9. **Most important open fact for the next session:** NOTHING from any of the five frontend UI passes (visual polish, round table, premium glass, declutter, background+hand-wrap) has been committed. It's all sitting as one large, continuously-growing uncommitted diff on top of commit `023d7bd`, currently touching `frontend/index.html`, `frontend/src/App.css`, `frontend/src/App.jsx`, `frontend/src/components/Card.jsx`, `frontend/src/index.css`, `frontend/src/screens/GameScreen.jsx`, plus new untracked `frontend/src/assets/table/`, `docs/phase-prompts/*.md`, and `docs/phase-prompts/design-refs/`. The user has been asked to commit this at least twice and has deferred each time to keep iterating live first. Get this actually committed as the first order of business next session, ideally after confirming the "one thing remaining" from item 3 either way — the longer this diff grows uncommitted, the more a single bad `git checkout`/crashed editor could cost.

10. Priority order for next session: (1) confirm with the user what "just one thing remaining" actually means (the ~21px overflow is a guess, not confirmed), fix it if so; (2) commit the accumulated frontend work — split into logical commits (art/typography, round-table+dealing, glass+declutter+hand-wrap) or one big one, ask the user's preference; (3) whatever the user's actual next priority is — this session ended with the UI work considered essentially done by the user, so it's likely either a final playtest with the friend group, or a new feature/polish area not yet scoped.
